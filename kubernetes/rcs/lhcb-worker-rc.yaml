apiVersion: v1
kind: ReplicationController
metadata:
  name: lhcb-worker
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: lhcp-worker
    spec:
      containers:
       - name: celery-worker
         image: diegodelemos/capreuse_worker:latest
         env:
          - name: QUEUE_ENV
            value: lhcb-queue
