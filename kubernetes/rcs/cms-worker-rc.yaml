apiVersion: v1
kind: ReplicationController
metadata:
  name: cms-worker
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: cms-worker
    spec:
      containers:
       - name: celery-worker
         image: diegodelemos/capreuse_worker:latest
         env:
          - name: QUEUE_ENV
            value: cms-queue